extends HTNAgentSensors

## This method is called on startup so you can do any initial setup, like
## listening to signals and setting initial world state
func _initialize(actor: Node) -> void:
	pass
